{
  "name": "filter3",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ filter3",
    "create-local": "graph create --node http://localhost:8020/ filter3",
    "remove-local": "graph remove --node http://localhost:8020/ filter3",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 filter3",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.3",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
